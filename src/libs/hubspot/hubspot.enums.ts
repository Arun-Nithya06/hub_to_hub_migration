// Account Types
export enum HubspotAccount {
  SOURCE = 'SOURCE',
  DESTINATION = 'DESTINATION',
}

// Object Types
export enum HubspotObject {
  CONTACTS = 'contacts',
  COMPANIES = 'companies',
  DEALS = 'deals',
  TICKETS = 'tickets',
  PRODUCTS = 'products',
  QUOTES = 'quotes',
  LINE_ITEMS = 'line_items',
  NOTES = 'notes',
  TASKS = 'tasks',
  MEETINGS = 'meetings',
  CALLS = 'calls',
  EMAILS = 'emails',
}
